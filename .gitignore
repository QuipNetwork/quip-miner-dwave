__pycache__/
*.pyc
dist/
build/
# PyInstaller scratch (see pyinstaller/quip-dwave-qa.spec)
.pyi-build/
*.egg-info/
.venv/
.env

# Agent tooling state and assistant-specific instructions. AGENTS.md is the
# cross-tool standard and is intentionally not ignored — it belongs in git.
.serena/
.claude/
.codex/
CLAUDE.md

# Local operator artifacts, never commit
leap_usage_report_*.csv
qpu-1-logs/
*.log
*.log.[0-9]
